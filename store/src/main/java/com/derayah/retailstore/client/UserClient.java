/*
package com.dreya.retailstore.client;

import com.dreya.retailstore.model.User;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestHeader;

*/
/**
 * UserClient.
 *
 * @author : Mo Sayed
 * @since : 5/24/2022
 *//*

@FeignClient(name = "userFeignClient", url = "${client.user.baseUrl}")
public interface UserClient {
    @GetMapping(value = "token")
    User getUser(@RequestHeader(value = "Authorization", required = true) String authorizationHeader);
}
*/
